# Reasonix Bridge MCP — 完整功能设计手册

> **版本**: v2.0 | **最后更新**: 2025-07  
> **架构**: Codex (决策层) ↔ Reasonix Bridge (路由层) ↔ DeepSeek (执行层)  
> **协议**: MCP stdio | **语言**: Node.js ≥ 18

---

## 目录

1. [架构总览](#一架构总览)
2. [核心链路与数据流](#二核心链路与数据流)
3. [6 个 MCP 工具详解](#三6-个-mcp-工具详解)
4. [二阶段执行引擎](#四二阶段执行引擎)
5. [多智能体并行执行](#五多智能体并行执行)
6. [4 大增强特性](#六4-大增强特性)
7. [improve_file 熔断机制](#七improve_file-熔断机制)
8. [8 个本地沙箱工具](#八8-个本地沙箱工具)
9. [安全机制](#九安全机制)
10. [环境变量与配置](#十环境变量与配置)
11. [快速启动与集成](#十一快速启动与集成)
12. [项目文件结构](#十二项目文件结构)
13. [技术指标与性能](#十三技术指标与性能)
14. [错误处理与恢复](#十四错误处理与恢复)
15. [附录：设计决策](#十五附录设计决策)

---

## 一、架构总览

### 1.1 三层架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Codex（决策层）                            │
│  用户交互 · 任务决策 · 工具选择 · 熔断接管                    │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  MCP 客户端（6 个工具入口）                           │   │
│  └──────────────────────┬───────────────────────────────┘   │
└─────────────────────────┼───────────────────────────────────┘
                          │ MCP stdio 协议 (JSON-RPC)
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              Reasonix Bridge（路由层）                        │
│  index.js (~1400 行)                                         │
│                                                              │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │  MCP 路由    │  │  调度引擎    │  │  沙箱安全层      │   │
│  │  · 工具分发  │  │  · 二阶段    │  │  · 路径校验     │   │
│  │  · 参数校验  │  │  · 多智能体  │  │  · 命令模式     │   │
│  │  · 结果返回  │  │  · 预算分配  │  │  · TOCTOU 防护  │   │
│  └─────────────┘  └──────────────┘  └──────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  HTTP 客户端 → DeepSeek API (chat/completions)       │   │
│  └──────────────────────┬───────────────────────────────┘   │
└─────────────────────────┼───────────────────────────────────┘
                          │ HTTP (REST)
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              DeepSeek（执行层）                                │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Agent Loop（最多 50 轮工具调用）                     │   │
│  │  · 规划阶段：文本模式，无工具                          │   │
│  │  · 执行阶段：8 个沙箱工具可用                         │   │
│  │  · 自动验证：文件存在性 + 语法检查                    │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  科学家团队（多智能体并行）                           │   │
│  │  🖥️ Dijkstra · 🎨 Hopper · 🗄️ Turing               │   │
│  │  🔗 Berners-Lee · 🧪 Lovelace                       │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 设计原则

| 原则 | 说明 |
|:-----|:------|
| **分层解耦** | Codex 只做决策，DeepSeek 只做执行，Bridge 负责路由和安全 |
| **增量安全** | 已存在的文件先读后改，绝不盲目覆盖 |
| **预算可控** | 累计 Token 上限 + 动态重分配，避免无限消耗 |
| **部分成功** | 前序步骤落盘后不受后续失败影响 |
| **熔断降级** | 5 次尝试失败后 Codex 直接接管 |

---

## 二、核心链路与数据流

### 2.1 完整请求生命周期

```
用户输入需求
    │
    ▼
Codex 选择工具（execute_task / improve_file / plan_task ...）
    │
    ▼
Bridge 接收 MCP 请求
    ├─ 参数校验（类型、必填、范围）
    ├─ 模式检查（命令模式是否允许）
    ├─ 预算检查（累计 Token 是否超限）
    │
    ▼
Bridge 构建 System Prompt（reasonix-prompt.md + 动态上下文）
    ├─ 注入增量模式信息（已存在文件列表）
    ├─ 注入命令模式限制
    └─ 注入预算上限
    │
    ▼
Bridge 调用 DeepSeek API（streaming）
    ├─ 实时解析 [progress] 标签 → 输出进度
    ├─ 实时解析 tool_use 块 → 执行沙箱工具
    └─ 累计 Token 计数
    │
    ▼
DeepSeek 返回最终结果
    │
    ▼
Bridge 后处理
    ├─ 提取接口摘要（供后续智能体通信）
    ├─ 验证文件完整性
    └─ 格式化 MCP 响应
    │
    ▼
Codex 收到结果 → 展示给用户
```

### 2.2 数据流图

```
                    ┌──────────────┐
                    │  用户需求     │
                    └──────┬───────┘
                           │
              ┌────────────▼────────────┐
              │   Codex 决策引擎        │
              │   · 分析需求复杂度      │
              │   · 选择 MCP 工具       │
              │   · 设置参数            │
              └────────────┬────────────┘
                           │ MCP Request
              ┌────────────▼────────────┐
              │   Bridge 路由层         │
              │   · 参数校验            │
              │   · 安全过滤            │
              │   · 预算检查            │
              └────────────┬────────────┘
                           │ HTTP Request
              ┌────────────▼────────────┐
              │   DeepSeek API          │
              │   · 流式响应            │
              │   · 工具调用            │
              └────────────┬────────────┘
                           │ Streaming Response
              ┌────────────▼────────────┐
              │   Bridge 解析层         │
              │   · 解析 tool_use       │
              │   · 执行沙箱工具        │
              │   · 累计 Token          │
              └────────────┬────────────┘
                           │ Tool Result
              ┌────────────▼────────────┐
              │   DeepSeek 继续推理     │
              │   · 分析工具结果        │
              │   · 决定下一步          │
              └────────────┬────────────┘
                           │ 循环直到完成
                           ▼
                    ┌──────────────┐
                    │  最终结果     │
                    └──────────────┘
```

---

## 三、6 个 MCP 工具详解

### 3.1 工具总览

| 工具 | 用途 | 复杂度 | 推荐场景 |
|:-----|:-----|:------:|:---------|
| **`execute_task`** ⭐ | **统一入口**，自动判断复杂度 | 自动 | **日常首选** |
| **`plan_task`** | 出方案不出代码 | 低 | 先看计划再动手 |
| **`orchestrate_task`** | 强制多智能体并行 | 高 | 明确需要分工的大项目 |
| **`improve_file`** | 改代码，支持熔断 | 中 | 发现代码需要调整时 |
| **`execute_step`** | 执行指定的一步 | 中 | 手动控制执行节奏 |
| **`delegate_to_reasonix`** | 旧版一键执行（兼容） | 中 | 简单小任务 |

### 3.2 execute_task（统一入口）

**功能**: 自动判断任务复杂度，选择单智能体或多智能体模式。

**参数**:

| 参数 | 类型 | 必填 | 说明 |
|:-----|:-----|:----:|:------|
| `task` | string | ✅ | 任务描述 |
| `context` | string | ❌ | 附加上下文信息 |

**自动决策逻辑**:

```
输入 task
  ├─ 涉及 >3 个文件？ → 多智能体模式
  ├─ 涉及多个领域（前后端/数据）？ → 多智能体模式
  ├─ 任务描述 < 100 字且单一领域？ → 单智能体模式
  └─ 默认 → 单智能体模式
```

**返回示例**:

```json
{
  "ok": true,
  "summary": "创建了博客系统，包含 5 个文件",
  "files": ["src/server.js", "src/routes.js", "src/models.js"],
  "agents": 3,
  "tokens_used": 45000,
  "duration_ms": 32000
}
```

### 3.3 plan_task（出方案）

**功能**: DeepSeek 以文本模式（无工具）分析任务，返回 JSON 步骤列表。

**参数**:

| 参数 | 类型 | 必填 | 说明 |
|:-----|:-----|:----:|:------|
| `task` | string | ✅ | 任务描述 |
| `detail` | string | ❌ | 详细程度：brief/normal/detailed |

**返回示例**:

```json
{
  "ok": true,
  "plan": [
    {"id": 1, "file": "src/server.js", "action": "创建 Express 服务器", "depends_on": []},
    {"id": 2, "file": "src/routes.js", "action": "创建 API 路由", "depends_on": [1]},
    {"id": 3, "file": "src/models.js", "action": "创建数据模型", "depends_on": [1]}
  ],
  "estimated_tokens": 35000
}
```

### 3.4 orchestrate_task（强制并行）

**功能**: 强制启用多智能体模式，拆解任务分配给科学家团队。

**参数**:

| 参数 | 类型 | 必填 | 说明 |
|:-----|:-----|:----:|:------|
| `task` | string | ✅ | 任务描述 |
| `agents` | string[] | ❌ | 指定科学家：["dijkstra","hopper","turing"] |
| `max_parallel` | number | ❌ | 最大并行数，默认 4 |

### 3.5 improve_file（改代码）

**功能**: 读取文件并修改，支持 attempt 熔断机制。

**参数**:

| 参数 | 类型 | 必填 | 说明 |
|:-----|:-----|:----:|:------|
| `file` | string | ✅ | 要修改的文件路径 |
| `idea` | string | ✅ | 改进想法（不是具体实现） |
| `attempt` | number | ✅ | 当前尝试次数，1~5 |
| `context` | string | ❌ | 附加上下文 |

**熔断逻辑**:

```
attempt=1 → DeepSeek 尝试修改
attempt=2 → 重试（带上次错误信息）
attempt=3 → 重试（带更多上下文）
attempt=4 → 重试（带完整错误日志）
attempt=5 → ⚠️ 熔断：返回 "已达最大尝试次数，请 Codex 直接修改"
```

### 3.6 execute_step（分步执行）

**功能**: 配合 plan_task 分步执行指定步骤。

**参数**:

| 参数 | 类型 | 必填 | 说明 |
|:-----|:-----|:----:|:------|
| `step_id` | number | ✅ | 步骤 ID |
| `plan` | object[] | ✅ | 完整计划 |
| `completed` | object[] | ❌ | 已完成步骤的摘要 |

### 3.7 delegate_to_reasonix（兼容旧版）

**功能**: 旧版一键执行，单次 agent loop。

**参数**:

| 参数 | 类型 | 必填 | 说明 |
|:-----|:-----|:----:|:------|
| `task` | string | ✅ | 任务描述 |

---

## 四、二阶段执行引擎

### 4.1 设计动机

传统单次 agent loop 的问题：
- **超时风险**: 长任务可能在中途超时，所有工作丢失
- **上下文污染**: 后续步骤可能意外修改前序文件
- **预算失控**: 无法精确控制每步的 Token 消耗

### 4.2 Phase 1：规划阶段

**模式**: 文本模式（不开放任何工具）

**输入**: 任务描述 + 增量模式信息（已存在文件列表）

**输出**: JSON 步骤列表

**特点**:
- 速度快：5~10 秒
- 消耗少：~8,000 tokens
- 生成清晰的执行路线图

**规划 JSON 格式**:

```json
[
  {
    "id": 1,
    "file": "src/main.py",
    "action": "创建主程序入口",
    "description": "创建 Express 服务器，监听 3000 端口",
    "depends_on": []
  },
  {
    "id": 2,
    "file": "src/utils.py",
    "action": "创建工具函数",
    "description": "实现日期格式化、字符串处理等工具函数",
    "depends_on": [1]
  },
  {
    "id": 3,
    "file": "src/routes.py",
    "action": "创建 API 路由",
    "description": "实现 CRUD 路由",
    "depends_on": [1, 2]
  }
]
```

### 4.3 Phase 2：执行阶段

**每步执行流程**:

```
Step N 开始
  │
  ├─ 新建 agent loop（最多 15 轮）
  ├─ 传递已完成的前置步骤摘要
  ├─ 可调用 8 个沙箱工具
  │
  ├─ 执行中...
  │   ├─ read_file（读取依赖文件）
  │   ├─ write_file（创建/修改文件）
  │   ├─ run_command（安装依赖/运行测试）
  │   └─ ...
  │
  ├─ 完成后自动验证
  │   ├─ 文件存在性检查
  │   ├─ 语法检查（node --check / tsc --noEmit）
  │   └─ 内容完整性检查
  │
  └─ 结果落盘 → 提取接口摘要 → 传递给下一步
```

**关键优势**:

```
✅ 前序步骤的文件已写入磁盘
✅ 即使后续步骤超时或失败，已完成部分不受影响
✅ 每步独立上下文，避免污染
✅ 每步独立预算控制
```

### 4.4 验证机制

| 验证类型 | 触发条件 | 检查内容 |
|:---------|:---------|:---------|
| 文件存在性 | 每步完成后 | 声明的文件是否已创建 |
| 语法检查 | 文件为 .js/.ts/.py | node --check / tsc --noEmit / python -m py_compile |
| 内容完整性 | 文件 > 100 行 | 检查是否有明显截断或占位符 |
| 接口一致性 | 多智能体模式 | 接口签名是否匹配 |

---

## 五、多智能体并行执行

### 5.1 科学家团队

| Emoji | 科学家 | 角色 | 专长 | 典型任务 |
|:-----:|:------|:----|:------|:---------|
| 🖥️ | **Dijkstra** | 后端 | 服务器、API、算法、业务逻辑 | Express/FastAPI 服务器 |
| 🎨 | **Hopper** | 前端 | UI、组件、交互、样式 | React/Vue 组件 |
| 🗄️ | **Turing** | 数据 | 数据库、存储、Schema | SQL/NoSQL 模型 |
| 🔗 | **Berners-Lee** | 接口 | API 通信、网络协议 | REST/GraphQL 接口 |
| 🧪 | **Lovelace** | 测试 | 测试、验证、部署 | Jest/Pytest 测试 |

### 5.2 并行调度逻辑

```
任务拆解
    │
    ▼
构建依赖图（DAG）
    │
    ▼
找出无依赖的根节点
    │
    ▼
Promise.all 并行执行根节点
    │
    ├─ 🖥️ Dijkstra（无依赖）→ 执行中...
    ├─ 🎨 Hopper（无依赖）→ 执行中...
    │
    ▼
等待所有根节点完成
    │
    ▼
提取接口契约
    │
    ▼
找出下一层（依赖已满足）
    │
    ▼
Promise.all 并行执行
    │
    ├─ 🗄️ Turing（依赖 Dijkstra）→ 执行中...
    │
    ▼
重复直到所有节点完成
```

### 5.3 单领域复杂任务的深度拆解

如果某个科学家的任务包含 >3 个文件，自动再拆成子小队：

```
🖥️ Dijkstra（任务复杂，含 5 个模块）
  ├─ 🖥️ Dijkstra-A  用户认证模块（auth.js）
  ├─ 🖥️ Dijkstra-B  文章 CRUD（posts.js）
  ├─ 🖥️ Dijkstra-C  评论系统（comments.js）
  └─ 🖥️ Dijkstra-D  文件上传（upload.js）
```

子小队之间同样遵循依赖关系：
```
Dijkstra-A（无依赖）
Dijkstra-B（依赖 A）
Dijkstra-C（依赖 A, B）
Dijkstra-D（依赖 A）
```

### 5.4 智能体通信协议

每个科学家完成时自动提取接口摘要，传递给后续科学家：

```
🖥️ Dijkstra-A 完成
  └─ 提取接口摘要:
      {
        "exports": ["createUser", "authenticate"],
        "api_routes": ["POST /api/users", "POST /api/auth"],
        "data_format": "JSON with {id, name, email}"
      }

→ 传递给 🎨 Hopper（前端）:
  "对接 /api/users 和 /api/auth 接口，使用 JSON 格式"
```

### 5.5 限制

| 限制项 | 值 | 说明 |
|:-------|:---:|:------|
| 最大子智能体数 | **12** 个 | 防止过度并行导致质量下降 |
| 每个子智能体最大轮次 | **10** 轮 | 防止单个智能体无限循环 |
| 每个子智能体最小预算 | **50,000** tokens | 确保有足够资源完成任务 |
| 深度拆解最多子小队 | **4** 个 | 防止拆解过细 |
| 最大并行数 | **4** 个 | 控制 API 并发 |

---

## 六、4 大增强特性

### 6.1 实时进度反馈

执行过程中通过 `[progress]` 标签输出状态：

```
[progress] 🚀 开始:创建博客系统...
[progress] 📋 拆解任务... 检测到 3 个模块
[progress] ▶️ 1/3 子智能体启动...
[progress]   ├─ 🖥️ Dijkstra ✅ 2轮 25s · 创建了 server.js
[progress] ▶️ 2/3 子智能体启动...
[progress]   ├─ 🎨 Hopper ✅ 3轮 18s · 创建了 App.jsx
[progress] ▶️ 3/3 子智能体启动...
[progress]   ├─ 🗄️ Turing ✅ 1轮 12s · 创建了 schema.sql
[progress] 📊 进度: 3/3 · 55s
[progress] ✅ 全部完成 · 58s
```

**进度标签格式**:

```
[progress] <emoji> <消息>
[progress]   ├─ <emoji> <科学家> <状态> <轮次> <耗时> · <摘要>
[progress] 📊 进度: <完成数>/<总数> · <总耗时>
[progress] ✅ 全部完成 · <总耗时>
```

### 6.2 增量模式

**检测逻辑**:

```
执行前：
  ├─ 遍历所有目标文件路径
  ├─ 检查文件是否已存在
  └─ 已存在 → 标记为 "增量模式"

执行中：
  ├─ 增量标记的文件 → 提示 DeepSeek 先 read_file
  ├─ 新文件 → 直接 write_file
  └─ 混合模式 → 分别处理
```

**System Prompt 注入**:

```
以下文件已存在，请先 read_file 再修改，不要盲目覆盖：
  - src/server.js（已存在，需先读取）
  - src/routes.js（已存在，需先读取）
  - src/models.js（新文件，可直接创建）
```

### 6.3 动态预算分配

**算法**:

```
初始分配：
  总预算 = REASONIX_MAX_TOKENS（默认 300,000）
  每个子智能体 = 总预算 / 子智能体数

运行中调整：
  当某个子智能体完成时：
    剩余预算 = 该智能体未使用的预算
    重新分配给未完成的智能体
    分配方式：均分

触发条件：
  子智能体完成 → 立即触发重分配
  子智能体失败 → 预算回收，不分给其他（防止连锁失败）
```

**示例**:

```
初始：3 个子智能体，总预算 300,000
  Dijkstra: 100,000
  Hopper: 100,000
  Turing: 100,000

Dijkstra 完成，只用 60,000（剩余 40,000）
  → 重分配：
    Hopper: 100,000 + 20,000 = 120,000
    Turing: 100,000 + 20,000 = 120,000

Hopper 完成，只用 80,000（剩余 40,000）
  → 重分配：
    Turing: 120,000 + 40,000 = 160,000
```

### 6.4 智能体通信

**接口摘要格式**:

```json
{
  "agent": "dijkstra",
  "files": ["src/server.js"],
  "exports": {
    "functions": ["createApp", "startServer"],
    "classes": ["App"],
    "types": ["User", "Post"]
  },
  "api_routes": [
    {"method": "GET", "path": "/api/users"},
    {"method": "POST", "path": "/api/users"}
  ],
  "data_contract": {
    "input": "JSON with {name, email}",
    "output": "JSON with {id, name, email, created_at}"
  }
}
```

**传递机制**:

```
科学家 A 完成
  → 提取接口摘要
  → 存入 sharedContext
  → 科学家 B 启动时注入
  → B 可以 read_file 读取 A 的文件
  → B 根据接口摘要编写对接代码
```

---

## 七、improve_file 熔断机制

### 7.1 设计目标

```
写代码任务可优先委托 DeepSeek 定向执行；失败、不满足要求或达到熔断阈值时，Codex/Bridge 直接接管
  → 改不好？improve_file attempt +1
  → 到 5 次还不行？熔断，Codex 直接接手
  → 下一个新任务？重置，又从 attempt=1 开始
```

### 7.2 完整流程

```
Codex 发现代码需要改进
    │
    ▼
improve_file(file, idea, attempt=1)
    │
    ├─ Bridge 读取文件内容
    ├─ 构建 prompt（文件内容 + 改进想法）
    ├─ 调用 DeepSeek
    ├─ DeepSeek 返回修改后的内容
    ├─ Bridge 应用修改
    │
    ▼
Codex 检查结果
    ├─ 满意？→ 完成 ✅
    └─ 不满意？
        │
        ▼
        improve_file(file, idea, attempt=2)
        │
        ├─ Bridge 读取文件（已修改的版本）
        ├─ 构建 prompt（文件内容 + 改进想法 + 上次错误）
        ├─ 调用 DeepSeek
        │
        ▼
        ... 重复直到 attempt=5
            │
            ▼
            ⚠️ 熔断信号：
            "已达最大尝试次数（5/5），请 Codex 直接修改"
            Codex 接管，直接编辑文件
```

### 7.3 参数说明

| 参数 | 类型 | 必填 | 说明 |
|:-----|:-----|:----:|:------|
| `file` | string | ✅ | 要修改的文件路径 |
| `idea` | string | ✅ | 改进想法/目标（不是具体实现） |
| `attempt` | number | ✅ | 当前是第几次尝试，从 1 开始，最大 5 |
| `context` | string | ❌ | 可选，附加上下文（如错误日志） |

### 7.4 熔断信号格式

```json
{
  "ok": false,
  "error": "improve_file_max_attempts",
  "message": "已达最大尝试次数（5/5），请 Codex 直接修改",
  "file": "src/server.js",
  "attempts": 5,
  "last_error": "路由注册失败，端口被占用"
}
```

---

## 八、8 个本地沙箱工具

### 8.1 工具总览

这些工具仅由 DeepSeek 在 agent loop 中调用，不直接暴露给 Codex：

| 工具 | 功能 | 需要模式 | 使用频率 |
|:-----|:-----|:--------|:--------:|
| `read_file` | 读文件（支持 head/tail/range） | 任意模式 | ⭐⭐⭐⭐⭐ |
| `search_content` | 搜索文件内容（支持正则） | 任意模式 | ⭐⭐⭐⭐ |
| `list_directory` | 列出目录 | 任意模式 | ⭐⭐⭐ |
| `glob` | 按 glob 匹配查找文件 | 任意模式 | ⭐⭐⭐ |
| `get_file_info` | 获取文件元数据 | 任意模式 | ⭐⭐ |
| `run_command` | 执行系统命令 | 按模式分级 | ⭐⭐⭐⭐ |
| `edit_file` | SEARCH/REPLACE 编辑 | `full` 模式 | ⭐⭐⭐⭐⭐ |
| `write_file` | 创建或覆盖文件 | `full` 模式 | ⭐⭐⭐⭐⭐ |

### 8.2 工具详解

#### read_file

```
用途：读取文件内容
参数：
  path    - 文件路径（必填）
  head    - 读取前 N 行（可选）
  tail    - 读取后 N 行（可选）
  range   - 读取指定范围，如 "10-20"（可选）

安全：路径经过 sandboxPath() 校验
```

#### search_content

```
用途：搜索文件内容
参数：
  pattern       - 搜索模式（必填）
  path          - 搜索目录（可选，默认 workspace）
  glob          - 文件过滤，如 "*.js"（可选）
  context       - 上下文行数（可选，默认 0）
  regex         - 是否使用正则（可选，默认 false）
  case_sensitive - 是否区分大小写（可选，默认 false）

安全：使用 Node.js 原生搜索，不调用 shell
```

#### list_directory

```
用途：列出目录内容
参数：
  path - 目录路径（必填）

返回：文件名、类型（file/dir）、大小、修改时间
```

#### glob

```
用途：按 glob 模式查找文件
参数：
  pattern - glob 模式，如 "**/*.js"（必填）
  path    - 搜索根目录（可选）
  limit   - 最大返回数（可选，默认 100）

安全：路径限制在 workspace 内
```

#### get_file_info

```
用途：获取文件或目录的元数据
参数：
  path - 路径（必填）

返回：大小、类型、创建时间、修改时间、权限
```

#### run_command

```
用途：执行系统命令
参数：
  tool - 命令工具名（必填）
  args - 参数数组（必填）
  cwd  - 工作目录（可选）

安全：按命令模式分级限制
```

#### edit_file

```
用途：SEARCH/REPLACE 编辑文件
参数：
  path    - 文件路径（必填）
  search  - 要搜索的文本（必填）
  replace - 替换文本（必填）

安全：需要 full 模式
注意：search 必须精确匹配文件中的内容
```

#### write_file

```
用途：创建新文件或覆盖已有文件
参数：
  path    - 文件路径（必填）
  content - 文件内容（必填）

安全：需要 full 模式
注意：增量模式下已存在的文件会提示先 read_file
```

### 8.3 工具调用格式

DeepSeek 通过以下格式调用工具：

```xml
<｜｜DSML｜｜tool_calls>
<｜｜DSML｜｜invoke name="read_file">
<｜｜DSML｜｜parameter name="path" string="true">src/server.js